# Decision Log

**Version:** 2.3.0  
**Date:** April 11, 2026  
**Owner:** HLLMR LLC  
**Status:** ACTIVE  
**Doc Status Tag:** Implemented

## Scope

High-value decisions that shape near-term execution.

| ID | Date | Decision | Rationale | Impacted Docs |
|---|---|---|---|---|
| D-001 | 2026-03-22 | Treat Phase 0 baseline as accepted for intended scope. | Enables controlled transition from foundation to consolidation and alignment phases. | ../MASTER_PLAN.md, ../dev/PHASE_0_IMPLEMENTATION_PLAN_v4_2_1.md |
| D-002 | 2026-03-22 | Adopt formal documentation governance with authority order and decision rule. | Prevents plan drift and conflicting agent guidance. | ../DOC_GOVERNANCE.md, ../DOCUMENTATION_INDEX.md |
| D-003 | 2026-03-22 | Require one Doc Status Tag in all docs markdown files. | Makes lifecycle state explicit and machine-auditable. | ../DOC_GOVERNANCE.md |
| D-004 | 2026-03-22 | Retire prototype-live/prototype-postmortem tree references after migration extraction. | Prevents stale links now that durable constraints are consolidated into canonical docs. | ../DOC_GOVERNANCE.md, ../DOCUMENTATION_INDEX.md |
| D-005 | 2026-03-22 | Retire all Phase C legacy merge-target docs after migration extraction. | Removes dead links and eliminates authority ambiguity from deprecated legacy sources. | ../DOC_GOVERNANCE.md, ../DOCUMENTATION_INDEX.md |
| D-006 | 2026-03-22 | Require section-level migration closure mapping for Phase D imports. | Makes migration decisions auditable from source insight to canonical section destination. | ../DOC_GOVERNANCE.md |
| D-007 | 2026-03-22 | Accept Phase 0 foundation stubs (engine extractor/processor, extract_page) as known open gaps rather than blocking Phase D doc work. | Docs can be canonical and accurate while code is in progress; capability-matrix must reflect actual state, not aspirational. | ../current-state/gap-register.md, ../current-state/capability-matrix.md |
| D-008 | 2026-03-22 | The Gap Register (gap-register.md) is the authoritative list of open implementation gaps; capability-matrix Open Gaps column links to it. | Single source of truth for gap tracking prevents drift between docs and code state. | ../current-state/gap-register.md, ../current-state/capability-matrix.md |
| D-009 | 2026-03-23 | Promote autonomous deterministic ROI detection to first-class strategy; keep manual ROI/profile management as admin-only refinement and controlled fallback. | Prototype constraints from JS/pdf-lib no longer apply; policy must align with deterministic extraction goals and reduce manual setup burden while preserving auditability. | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../PHASE_05_HANDOFF.md |
| D-010 | 2026-03-23 | Add Intake Triage as a mandatory pre-Lexer Stage 0 producing a `NormalizedIntakeBundle` from raw user inputs. | Multi-file ingestion, page auditing, rotation normalization, and optional explicit-invoke classification require a dedicated normalization stage before extraction begins; this prevents messy inputs from propagating silent errors into the Lexer. | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../PHASE_05_HANDOFF.md |
| D-011 | 2026-03-23 | Designate `lopdf` (MIT-licensed, pure Rust) as the canonical PDF write backend for all page-level operations (insert, delete, swap, rotation normalization, bookmark mutation). | PDFium is read-only; Headless Chrome generates only new content. A write-capable library is required for PdfStitcher (Phase 6) and Intake Triage rotation correction; `lopdf` is the only candidate consistent with Non-Negotiable #10 (no Python runtime) and #11 (no GPL). | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-012 | 2026-03-23 | Revise Non-Negotiable #18 to permit autonomous document-type classification when the user explicitly invokes a classify, split, or normalize intake operation. | #18 was written to prevent silent auto-selection into a wrong processing workflow, not to prohibit classification as the explicit response to a declared user intent. The revision preserves the prohibition against silent workflow selection while enabling robust intake normalization. | ../MASTER_PLAN.md |
| D-013 | 2026-03-23 | Adopt vector-first deterministic adaptive detection as baseline for drawings/specs; ML is non-baseline fallback only. | Clean vector PDFs provide reliable geometry and text primitives in PDFium. Deterministic scoring + explainable tie-breaks meet auditability requirements while covering most inputs without opaque AI dependence. | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../PHASE_05_HANDOFF.md |
| D-014 | 2026-03-23 | Treat firm/layout templates as auto-learned internal accelerators, not manual ROI/profile authoring UX. | Repeated firm title-block structures allow high-confidence reuse of relative ROIs. Keeping templates internal preserves deterministic behavior while removing profile-management burden from primary user workflows. | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../PHASE_05_HANDOFF.md |
| D-015 | 2026-03-23 | Restrict AI-enhanced fallback to explicit opt-in, low-confidence-only, cropped-region scope. | Maintains privacy posture and deterministic baseline while allowing bounded recovery path for edge cases. Prevents silent cloud dependence and minimizes data transfer scope. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-016 | 2026-03-23 | Add local micro-ML confidence-assist policy for hard edge cases, bounded by deterministic baseline and version-locked inference. | Preserves deterministic baseline while improving edge-case coverage; model/version/fusion auditability prevents opaque drift. | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../PHASE_05_HANDOFF.md |
| D-017 | 2026-03-23 | Add power-user API-driven LLM integration for ambiguity validation and explicit instruction-set generation. | Enables advanced users to resolve ambiguous inputs with optional assisted workflows while keeping standard pipeline deterministic and explainable. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-018 | 2026-03-23 | Promote raster OCR to first-class pipeline path for low-text/raster PDFs with source-confidence attribution. | Raster-heavy inputs cannot rely on vector text extraction. OCR path must be explicit, confidence-scored, and provenance-preserving to protect reliability. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-019 | 2026-03-23 | Require schedule/table parsing outputs to support schema-versioned JSON/CSV/XML export contracts. | Structured schedule extraction is a target workflow; exports must be machine-consumable, stable, and auditable across integrations. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-020 | 2026-03-23 | Promote replayable correction manifests and provenance-first review to first-class workflow primitives. | The trust moat is operational closure, not just extraction accuracy. Corrections must be replayable and every reviewable artifact must resolve to source evidence. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-021 | 2026-03-23 | Treat native diff/compare reports and focused exception triage as core deliverables for revision workflows. | Users need to review change and ambiguity efficiently; exception-first review is higher leverage than broad manual QA of all output. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-022 | 2026-03-23 | Standards normalization will extend the existing canonical UDS/NCS/MasterFormat scaffold rather than invent parallel mappings. | The prototype-recovered standards docs already define the normalization backbone; implementation should wire to that scaffold to avoid semantic drift. | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../AEC_STANDARDS.md, ../MASTERFORMAT_REFERENCE.md |
| D-023 | 2026-03-23 | Add batch orchestration with resumable jobs and per-file/per-page state accounting as a production requirement. | Partial-success philosophy requires explicit queueing, retry, and resume semantics to operate reliably at project scale. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-024 | 2026-03-23 | Standardize all executable automation through a typed instruction DSL with dry-run validation. | Human corrections, power-user recipes, and AI-suggested instructions must converge on one validated manifest surface to avoid divergent execution semantics. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-025 | 2026-03-23 | Expose confidence through user-facing policy profiles rather than raw internal thresholds. | Customers need stable operational modes without coupling UI semantics to low-level heuristic details that may evolve. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-026 | 2026-03-23 | Add cross-document entity resolution and searchable project knowledge indexing as strategic downstream capabilities. | Linking sheets, sections, tags, firms, and revisions across mediums expands the product from parser to project system of record. | ../MASTER_PLAN.md, ../ARCHITECTURE.md |
| D-027 | 2026-03-23 | Consolidate the committed feature set into an explicit phased priority stack: Phase 0.5 ships only Band 0 runtime and Band 0.5 contract-shaping; later runtime bands are deferred by design. | The feature surface is now broad enough to sprawl across Phase 0.5 unless runtime-vs-contract boundaries and post-0.5 landing zones are explicitly locked. | ../PHASE_05_HANDOFF.md, ../current-state/state-summary.md |
| D-028 | 2026-04-04 | Use `page.text().chars()` (PDFium `FPDFText_LoadPage` / `FPDFText_GetCharBox` pipeline) rather than `page.objects().iter()` for text extraction in `pattern-dev` detection functions. | `page.objects().iter()` yields only top-level page objects; PDF Form XObjects — the standard mechanism for running headers/footers in AEC spec templates — appear as a single opaque node, causing `as_text_object()` to return `None` for all footer text. `page.text().chars()` descends transparently into Form XObjects and returns page-coordinate bounds for every character, enabling footer and header detection on all document types. Validated: 499→5 failures on SPEC_RWB_LHHS_ALL_ORG.pdf (571 pages) after switching API. | ../../tools/pattern_dev.rs, ../current-state/gap-register.md |
| D-029 | 2026-04-05 | Retire `phase05Implementation.md` and integrate its content into `docs/PHASE_05_HANDOFF.md` Appendix A (Implementation History). | The implementation plan file served its purpose as execution scaffolding during Phase 0.5. With Phase L complete and all decisions, locked command surfaces, critical discoveries, and test inventory captured in `docs/PHASE_05_HANDOFF.md` Appendix A, the planning file is no longer needed and its retention as a separate root-level file would require ongoing maintenance. All dead links from D-009/D-010/D-013/D-014/D-016/D-027 updated to `../PHASE_05_HANDOFF.md`. | ../PHASE_05_HANDOFF.md, ../current-state/state-summary.md, ../DOCUMENTATION_INDEX.md |
| D-030 | 2026-04-06 | Use `std::process::Command` to invoke the Chromium binary directly rather than the `headless_chrome` crate for Phase 5 HTML→PDF rendering. | The `headless_chrome` crate adds significant dependency weight, has API stability concerns, and uses a CDP (Chrome DevTools Protocol) socket path that is fragile across Chrome versions. Invoking `chrome --headless --print-to-pdf` via `Command` is simpler, requires no additional crate dependencies, and is stable across all Chromium-family browsers. The output PDF is written to a temp file and read back as bytes. | ../../crates/engine/src/render/chrome_pdf.rs |
| D-031 | 2026-04-06 | Accept any Chromium-family browser (Chrome, Brave, Chromium, Edge) for the Phase 5 render path; search `CHROME_PATH` env var first, then well-known system paths including Brave. | The `--headless --print-to-pdf` flags and CSS Paged Media `@page` margin-box rules work identically across all Chromium-family browsers at Chrome 120+ equivalent. Brave is confirmed working in the dev environment (54 KB PDF, 42 s, full SPEC round-trip). Searching commonly-installed Chromium variants reduces the "Chrome not found" failure surface without adding any runtime dependency. | ../../crates/engine/src/render/chrome_pdf.rs |
| D-032 | 2026-04-06 | Capture PDF span geometry (x position, font size, line gap) in the parser and store it on `AstNode.x_indent` and `SectionAst.layout: Option<SectionLayout>` rather than discarding it after line clustering. | `cluster_lines()` already computed `x_min` per line but previously discarded it. Font size and y positions were already available from raw `Span` data. Retaining these lets the render pipeline derive accurate per-level `margin-left` CSS, `font-size`, and `line-height` from the actual source document geometry rather than hardcoded CSS defaults — directly improving Phase 5 regeneration fidelity. `#[serde(default)]` on both fields ensures zero breaking changes to existing serialized AST files. | ../../crates/ir/src/ast.rs, ../../crates/engine/src/parse.rs, ../../crates/engine/src/render/body.rs, ../../crates/engine/src/render/chrome.rs |
| D-033 | 2026-04-06 | Extend `SpanData` with `font_weight: f32` and `is_italic: bool` extracted from PDFium's `text_obj.font()` API; add `is_italic: bool` to the `Span` IR type; add `body_font_name: String` (modal font family) to `SectionLayout`; use `body_font_name` in the CSS `font-family` rule when available. | `font_weight` was available via `PdfFontWeight` enum but never extracted — it was silently hardcoded to 400 in `Span::new()`. `is_italic` and modal font name were similarly discarded. These three values are the remaining typographic properties PDFium exposes per-span that are directly useful for regeneration fidelity: font weight enables auditing bold-text consistency; italic preserves oblique distinctions in header/PART lines; `body_font_name` lets the renderer match the source PDF's typeface exactly rather than using a `RenderConfig` default. All via `#[serde(default)]` so no existing serialized files break. | ../../crates/pdf-extraction/src/extractor.rs, ../../crates/ir/src/types.rs, ../../crates/ir/src/ast.rs, ../../crates/engine/src/pipeline/extraction.rs, ../../crates/engine/src/parse.rs, ../../crates/engine/src/render/chrome.rs |
| D-034 | 2026-04-11 | Fix `drawing_segment.rs` line 126 title-block region filter from `\|\|` to `&&` (Sprint 10.5). | The `\|\|` guard was matching spec PDF footer text as potential sheet IDs (any span whose y-position OR font-size condition matched was included), causing `cli_index_drawing_on_non_drawing_fixture_completes_with_zero_sheets` to return 6 sheets instead of 0 on spec PDF fixtures. Changing to `&&` requires both conditions (y-position in bottom band AND font-size in title-block range) to apply, restoring deterministic zero-sheet output on non-drawing inputs. 19/19 drawing_segment unit tests pass after fix. | ../../crates/engine/src/drawing_segment.rs, ../../apps/backend-cli/tests/cli_integration_test.rs |
| D-035 | 2026-04-11 | Lock `TidyRow` / `EquipmentDataset` tidy export schema at `schema_version = "1.0.0"` (Sprint 10.4, closes G-028). | The submittal tidy export schema (`EquipmentDataset` with 14-column `TidyRow`: packet_name, revision_id, item_tag, equipment_type, section, field, value_raw, value_num, unit, page, bbox, confidence, source, conflict_flags) is now contractually stable. Any field additions require a minor version bump; removals or type changes require a major version bump. XML export is deferred to G-029 remainder. Future schema changes must update `EquipmentDataset.schema_version` and add a compatibility migration note in the decision log. | ../../crates/ir/src/submittal.rs, ../../crates/engine/src/submittal_export.rs, ../../apps/backend-cli/src/handlers/extract_submittal.rs |

## Decision Rule Reminder

If guidance is not in the master plan or canonical derived docs, treat it as non-authoritative.

Every future decision entry must reference at least one canonical destination doc.
