# Capability Matrix

**Version:** 2.2.0  
**Date:** April 7, 2026  
**Owner:** HLLMR LLC  
**Status:** ACTIVE  
**Doc Status Tag:** Implemented

## Scope

Implemented vs planned capabilities by crate and workflow.

Classification rule:

- `Implemented` requires code presence plus test coverage evidence.
- `Planned` requires canonical-source intent in master or derived canonical docs.

## Crate Capability Matrix

| Area | Implemented | Planned | Evidence | Open Gaps |
|---|---|---|---|---|
| `crates/ir` data model and validation | **Implemented** — `LayoutTranscript`, `Page`, `Span`, `BBox`, `SegmentIndex`, `SectionEntry`, `ParsedDocument`, `SectionAst`, `AstNode`, `OutlineTag` all implemented and tested; `validate_transcript()` enforced in engine pipeline; `Validator::validate()` delegates to `validate_transcript()` (G-007 closed); **Phase 5 render types**: `SpecChromeMetadata`, `RenderConfig`, `PageSize`, `RenderResult`, `RenderError` added in `crates/ir/src/render.rs`; **Phase 5 supplement**: `SectionLayout` struct (`body_left`, `body_right`, `font_size_pt`, `line_gap_norm`, `body_font_name`) and `x_indent: f64` on `AstNode`, `layout: Option<SectionLayout>` on `SectionAst` — all with `#[serde(default)]` for backward compatibility; **Phase 5.5 supplement**: `Span` gains `is_italic: bool` (`#[serde(default)]`); `Span.font_weight` now populated from PDFium (was hardcoded 400); **Phase 6 stitch types**: `StitchPlan`, `StitchResult`, `StitchError` added in `crates/ir/src/stitch.rs`; `Document` and `Element` stubs remain deferred (G-008 accepted) | Complete Document/Element types (Phase 7+ consumer) | `crates/ir/src/`, `crates/ir/tests/` | — |
| `crates/pdf-extraction` extraction baseline | **Implemented** — `PdfiumExtractor::extract_page()` returns real `PageData` with `Vec<SpanData>` (text, font name, font size, **font weight**, **is_italic**, `RawBBox`), `width_pts`, `height_pts`; `font_weight` extracted from `text_obj.font().weight()` (`PdfFontWeight` enum → f32); `is_italic` from `text_obj.font().is_italic()`; `load_document`, `get_page_count`, `extract_text` all implemented and tested; PDF→IR span conversion wired in engine extraction pipeline (G-005 closed) | — | `crates/pdf-extraction/src/extractor.rs`, `crates/pdf-extraction/src/types.rs` | — |
| `crates/engine` processing baseline | **Implemented through Phase 7 (apply-addendum) + layout geometry + font typography supplements** — extraction wired (`pipeline/extraction.rs`), validation wired (`pipeline/parsing.rs`, G-002 closed), segmentation engine (`segment.rs`): cluster-based footer CSI section ID detection with section title extraction, FOOTER_Y/FOOTER_CLUSTER_GAP constants, `detect_section_id()`, `extract_title_from_clusters()`, `inject_missing_parts()` recovery; parse engine (`parse.rs`): 5-level CSI outline tree (Part→Article→Paragraph→SubParagraph→SubSubParagraph→SubSubSubParagraph), y-proximity line clustering, x-sort, continuation folding, noise-skip; **layout geometry**: `cluster_lines()` returns `x_min` per line, `classify_lines()` + `build_tree()` propagate to `AstNode.x_indent`, `compute_section_layout()` derives `SectionLayout` (body margins, median font size, median line gap, modal font name via `modal_font_name()`); HTML visualizer (`visualize_ast.rs`); edit engine (`edit.rs`): `SectionEditor` with pre-flight validation, `apply_delete_to_section`, `apply_replace_to_section`, `apply_insert_after_to_section`, CSI-canonical `renumber_siblings`; **render engine** (`render/`): `SectionRenderer`, `build_body_html()` (AST→HTML fragment, 7 CSS classes, measured inline `margin-left` from `x_indent` when layout present), `build_full_html()` (CSS `@page` margin boxes, measured `font-size` + `line-height` + `font-family` from `SectionLayout` when present), `render_html_to_pdf()` (Chrome subprocess, `CHROME_PATH` or system path discovery); **stitch engine** (`stitch.rs`): `PdfStitcher::stitch()` — lopdf-based page-replacement algorithm (object renumber, `/Kids` splice, `/Parent` fixup, bookmark re-route, unchanged-page validation); **Phase 7 specs-patch orchestrator** (`specs_patch.rs`): `SpecsPatchOrchestrator::run()` — end-to-end Apply Addendum workflow combining extract/segment/parse/edit/render/stitch stages with partial-success semantics and 3-level chrome metadata merge; `backend-cli` handles: extract, visualize, segment, visualize-segments, parse, visualize-ast, edit, regenerate, stitch, **apply-addendum**; G-001, G-002, G-003 (accepted), G-005, G-006, G-007, G-008 (accepted), G-009, G-010, G-022 all closed; 41/41 integration tests pass (36 pre-Phase-7 + 5 Phase-7; 2 #[ignore]); main.rs prints version only (deliberate — G-003 accepted) | Phase 8+ intake triage, cross-medium addenda merge | `crates/engine/src/`, `apps/backend-cli/tests/cli_integration_test.rs` | — |
| `crates/audit` bundle/event writer baseline | **Integrated** — AuditEvent, AuditBundle, event types, JSON writer/reader all present; all 10 CLI handlers emit `OperationStarted`/`OperationEnded` events (extract, segment, parse, visualize, visualize-segments, visualize-ast, edit, regenerate, stitch, apply-addendum) — G-006 closed; behavioral tests added in Phase 5 pre-work (JSON round-trip, event ordering, count tracking, iter coverage) — G-010 closed | — | `crates/audit/src/` | — |
| `crates/contracts` downstream contract surfaces | **Implemented** — 6 modules: `intake`, `ocr_routing`, `schedule`, `assisted_intelligence`, `operational_trust`, `knowledge`; 24/24 unit tests (round-trip serde for every major type); all types `#[derive(Debug, Clone, Serialize, Deserialize)]`; **CONTRACT-ONLY** by design; no runtime execution wired | Runtime implementation in Band 1–4 (see gap assignments) | `crates/contracts/src/` | Runtime: G-013–G-016 (intake), G-027 (OCR), G-028–G-029 (schedule), G-023–G-026 (AI), G-030–G-036 (trust), G-037–G-039 (knowledge) |

## Workflow-Level Matrix (Canonical Intent)

| Workflow | Current State | Next Milestone | Source of Truth | Open Gaps |
|---|---|---|---|---|
| Deterministic extraction + edit + regenerate + stitch pipeline | **Implemented through Phase 7** — real PDFium extraction wired; `SpanData`→`Span` conversion with normalized coords (top-left origin, [0,1] range); `validate_transcript()` called in pipeline; extract + segment + parse + edit + regenerate + stitch pipeline operational end-to-end, then orchestrated end-to-end via Phase 7 apply-addendum workflow; all 10 handlers emit `OperationStarted`/`OperationEnded` audit events; **Phase 7 specs-patch**: `SpecsPatchOrchestrator::run()` applies full orchestrated pipeline sequence (Extract → Segment → Parse → Edit → Render → Stitch with last-to-first ordering); 41/41 integration tests pass | Phase 8+ intake normalization/triage | `crates/engine/src/pipeline/extraction.rs`, `crates/engine/src/edit.rs`, `crates/engine/src/render/`, `crates/engine/src/stitch.rs`, `crates/engine/src/specs_patch.rs`, `apps/backend-cli/tests/cli_integration_test.rs` | G-003 (accepted) |
| Section regeneration (AST → PDF) | **Implemented (Phase 5 + layout geometry supplement)** — `crates/engine/src/render/`: `body.rs` builds HTML fragment (7 CSS classes for all OutlineTag variants; inline `margin-left` CSS per node derived from measured `x_indent` and `SectionLayout.body_left` when layout is present), `chrome.rs` wraps with `<head>` + CSS `@page` running-header/footer rules plus measured `font-size` (from `SectionLayout.font_size_pt`) and `line-height` (derived from `SectionLayout.line_gap_norm * 11in * 72pt/in`, clamped 1.1–2.0), `chrome_pdf.rs` invokes Chrome subprocess (`CHROME_PATH` env or system discovery); `SectionRenderer::new(config).render(ast, chrome)` is the public API; `SectionRenderer::dry_run()` skips Chrome for CI/dry-run; 4/4 non-Chrome integration tests pass; 1 full-Chrome test available with `#[ignore]`; **Phase 7 orchestration**: now part of `SpecsPatchOrchestrator` pipeline for full apply-addendum workflow | Phase 8+ intake normalization | `crates/engine/src/render/`, `crates/engine/src/specs_patch.rs`, `apps/backend-cli/src/handlers/regenerate.rs`, `apps/backend-cli/tests/cli_integration_test.rs` | — |
| Validation and invariants | **Implemented** — `validate_transcript()` enforced in engine Processor; `Validator::validate()` delegates to it; G-002 and G-007 closed | Deeper invariant checks as new IR types added | `crates/engine/src/pipeline/parsing.rs`, `crates/ir/src/validation.rs` | G-008 (Document/Element stubs) |
| Auditability outputs | **Integrated at CLI handler level** — all 7 `backend-cli` handlers emit `OperationStarted`/`OperationEnded` `AuditEventData` events via `AuditBundle`; G-006 closed in Phase 4. Deep inline hooks in `Extractor::extract()` and `Processor::process()` deferred. | Deep inline audit hooks in extraction/processing internals | `apps/backend-cli/src/handlers/` | — |
| Autonomous ROI detection policy | **Not implemented** — no deterministic ROI candidate/ranking engine in runtime path | Implement deterministic candidate generation, ranking, diagnostics, and tie-break policy | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-011 |
| Admin-only manual ROI refinement | **Not implemented** — no admin override manifest or restricted operator flow yet | Add admin-only override/repair path without making manual profiles a baseline requirement | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-012 |
| Multi-file intake bundle assembly | **Not implemented** — `WorkflowRequest.input_path` is singular; no `IntakeBundle` or `IntakeIssue` types | Implement G-013 contract then G-014 batch addenda assembly | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-013, G-014 |
| Intake Triage: page audit and rotation normalization | **Not implemented** — no Stage 0 pre-Lexer pipeline; no rotation detection or `lopdf` write path | Implement Stage 0 Intake Triage: page audit, rotation detect/correct via `lopdf`, bundle manifest | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-016 |
| Autonomous document-type classification | **Not implemented** — no furniture-pattern-based page classification; classification manifest not defined | Implement G-015 after furniture patterns are stable (Phase 2-gated); advisory output only | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-015 |
| PDF section page replacement (stitch) | **Implemented (Phase 6)** — `PdfStitcher::stitch()` in `crates/engine/src/stitch.rs`; lopdf 0.40.0 write path; `stitch` CLI subcommand; `/Kids` splice + bookmark fixup; 6/6 integration tests pass | — | `crates/engine/src/stitch.rs`, `apps/backend-cli/tests/cli_integration_test.rs` | — |
| End-to-end addendum workflow (apply-addendum) | **Implemented (Phase 7)** — `SpecsPatchOrchestrator::run()` in `crates/engine/src/specs_patch.rs`; `ApplyAddendumHandler` in `apps/backend-cli/src/handlers/apply_addendum.rs`; `apply-addendum` CLI subcommand with `--original`, `--addendum` (JSON manifest), `--output`, `--audit-bundle` (optional), `--dry-run` flags; Extract → Segment → Parse → Edit → Render → Stitch (last-to-first) orchestration; partial-success semantics with per-section failure tracking; 3-level chrome metadata merge (base + manifest override + per-section override); 5 non-Chrome + 1 Chrome `#[ignore]` integration tests pass; `AddendumManifest`, `SectionEditSpec`, `SectionPatchResult`, `AddendumResult` IR types in `crates/ir/src/addendum.rs` with 12 unit tests | — | `crates/engine/src/specs_patch.rs`, `apps/backend-cli/src/handlers/apply_addendum.rs`, `apps/backend-cli/tests/cli_integration_test.rs` | — |
| Cross-medium addenda merge | **Not implemented** — `merge_addenda.rs` is a stub; no multi-medium workflow | Implement G-017 after G-014 and Phase 7 intake triage complete | ../MASTER_PLAN.md | G-017 |
| Drawing title-block localization (vector-first) | **Not implemented** — no frame/corner-band candidate generator or table-structure scoring pass exists | Implement G-018 then G-019 for deterministic title-block ROI selection and diagnostics | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-018, G-019 |
| Drawing sheet field extraction (sheet number/title) | **Not implemented** — no deterministic intra-title-block field scorer currently wired | Implement G-020 with pattern + typography + relative-position scoring and tie-break evidence | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-020 |
| Auto-learned firm/layout templates | **Not implemented** — no internal template store, status model, or drift checks | Implement G-021 schema and replay/drift contract; keep profile concepts out of primary UX | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-021 |
| Spec heading segmentation (vector-first) | **Implemented** — G-022 closed by Phase 3 parse engine: y-proximity line clustering (LINE_Y_EPSILON=0.012), span x-sort, PART/Article/Paragraph/Sub-paragraph regexes, continuation folding, noise-skip, `inject_missing_parts` recovery. 0.2% unclassified rate on 571-page corpus. | — | `crates/engine/src/parse.rs`, `crates/engine/src/segment.rs` | — |
| AI-enhanced fallback contract (optional) | **Not implemented** — no explicit opt-in gate or cropped-region payload manifest exists | Implement G-023 as optional fallback contract with audit events; non-baseline path | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-023 |
| Local micro-ML confidence assist | **Not implemented** — no on-device model runtime or version-pinned assist path integrated | Implement G-024 runtime path and G-025 deterministic fusion with audit provenance | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-024, G-025 |
| Power-user LLM validation/instruction API | **Not implemented** — no contract endpoints for ambiguity validation or instruction-set generation | Implement G-026 contracts and advisory-to-executable promotion guardrails | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-026 |
| Raster OCR extraction path | **Not implemented** — no low-text/raster routing and no `ocr` vs `vector` source tagging in transcript path | Implement G-027 OCR routing with confidence/provenance tags | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-027 |
| Schedule parser canonical schema | **Not implemented** — no table parser producing stable schema records with provenance | Implement G-028 canonical schedule schema and parser outputs | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-028 |
| Schedule export adapters (JSON/CSV/XML) | **Not implemented** — JSON/CSV/XML schedule export contracts and versioning are not wired | Implement G-029 export adapters with stable field mappings and schema version tags | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-029 |
| Replayable correction manifests | **Not implemented** — no typed correction manifest or deterministic replay/reapply path exists | Implement G-030 schema and replay engine with scope guards and divergence reporting | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-030 |
| Native diff/compare workflows | **Not implemented** — no structured spec/sheet/packet compare reports are produced | Implement G-031 compare workflows and integrate with review outputs | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-031 |
| Provenance-first review contract | **Not implemented** — no unified review object model links fields, decisions, and corrections back to evidence | Implement G-032 review contract with evidence refs and branch reasons | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-032 |
| Privacy/redaction outbound controls | **Not implemented** — no region-level redaction policy or outbound payload manifest for external API paths | Implement G-033 redaction policy, payload manifests, and privacy modes | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-033 |
| Batch orchestration and resumability | **Not implemented** — no job queue, retry, or resume semantics exist | Implement G-034 job/file/page state accounting and resumable orchestration | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-034 |
| Instruction DSL / manifest language | **Not implemented** — executable automation is not standardized on one typed manifest surface | Implement G-035 parser, validator, dry-run, and execution bridge | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-035 |
| Confidence policy profiles | **Not implemented** — no stable user-facing operational modes exist for review strictness and escalation behavior | Implement G-036 calibrated policy profiles and UI/contract surface | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-036 |
| Standards normalization wiring | **Not implemented** — existing canonical standards scaffold is documented but not yet wired into normalization runtime paths | Implement G-037 using existing UDS/NCS/MasterFormat references and raw-to-canonical mapping evidence | ../MASTER_PLAN.md, ../ARCHITECTURE.md, ../AEC_STANDARDS.md | G-037 |
| Cross-document entity resolution | **Not implemented** — no linked entity layer exists across sheets, sections, tags, firms, and revisions | Implement G-038 entity linking, evidence basis, and confidence model | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-038 |
| Project knowledge index and triage queue | **Not implemented** — no searchable knowledge surface, exception queue, or drift dashboard contract exists | Implement G-039 searchable index/triage contracts and metrics surface | ../MASTER_PLAN.md, ../ARCHITECTURE.md | G-039 |
| `tools` pattern-dev developer CLI | **Implemented** — all Phase 0.5 phases B–L complete; `inspect`, `test-pattern`, `validate-corpus` subcommands runtime-ready; `footer-section-id`/`page-counter`/`header-band` detection live; `title-block-anchor` partial runtime (corner scoring, Phase G bonus); `roi-candidate`/`spec-heading` schema-only; overlay PNGs at 1400 px; sidecar schema v0.5.0 locked; 32 unit + 5 integration tests passing; corpus validated: 27 Tier 1 fixtures, 2,892 pages, `det_regressions=0` | — | `tools/pattern_dev.rs`, `tools/src/pattern_model.rs`, `tools/tests/pattern_dev_integration.rs` | Band 1: G-011 (ROI ranking runtime), G-018–G-019 (title-block full scoring) |
| Documentation governance | **Implemented** — authority order, tags, triage matrix all active | Phase E/F gap closure; Phase D-ext continuation | ../DOC_GOVERNANCE.md | None |

## Notes

- Mark capability as implemented only when code plus tests exist.
- Planned entries must map to canonical docs, not archival references.
- Re-check this table at each phase boundary and update evidence paths if code moves.
- Open gap IDs (G-NNN) reference `../current-state/gap-register.md` for full detail and criticality classification.
